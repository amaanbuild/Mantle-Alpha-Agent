{
  "name": "mantle-alert-logger",
  "version": "1.0.0",
  "description": "On-chain alert logger contract for the Mantle Alpha Agent, deployable on Mantle testnet.",
  "license": "MIT",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network mantleTestnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network mantleMainnet",
    "verify": "hardhat run scripts/verify.js --network mantleTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  }
}
